//Commands: (in order from top to bottom) NOTE: as of V1.0.5, we moved to Ubuntu 22.04 to ensure compatibility with our Live ISO bypass, with custom package removals with LIVE ISO only
cd ..
cd ..
//first, we want to purge all the useless apps
sudo apt purge -y libreoffice-core libreoffice-common ure uno-libs-private libuno-purpenvhelpergcc3-3 libuno-cppu3 libuno-sal3 libreoffice-style-breeze libreoffice-style-colibre 
//Ubuntu specific iso reduction, we are stripping down the unused packages
apt purge snapd
apt autoremove
//Now, make it live boot only
apt remove ubiquity
apt autoremove --purge ubiquity ubiquity-casper ubiquity-frontend-gtk ubiquity-slideshow-ubuntu ubiquity-ubuntu-artwork
//now back to our regular customizations
sudo crontab -e
1
//enter password
//type in: @reboot/home/AUTOSTART/startup.sh
//ctrl x
//enter
cd home
mkdir AUTOSTART
cd AUTOSTART
touch startup.sh
chmod a+x /home/AUTOSTART/startup.sh
nano startup.sh
//Type in: #!/bin/sh
//Type in: find /home/AUTOSTART/CODE -maxdepth 1 -type f -exec cmd option {} \; > /home/AUTOSTART/OUTPUT/results.out
//ctrl x
//enter
mkdir CODE
cd CODE
chmod -R a+rwx /home/AUTOSTART/CODE
cd ..
//NOTE: in between here, you can add the code you want to autorun, mainly for test builds, i will put these in comments under here
//cd CODE
//touch TestPython.py
//nano TestPython.py
//type: print("This is python running...")
//ctrl x
//enter
//touch TestJava.java
//nano TestJava.java
//type: System.out.println("This is java running...");
//ctrl x
//enter
//touch TestC.c
//nano TestC.c
//type: #include<cstdlib> #include<iostream> int main(){ std::cout << "This is C running..." << std::endl; return 0; }
//ctrl x
//enter
//cd ..
mkdir OUTPUT
cd OUTPUT
touch results.out
chmod a+rw /home/AUTOSTART/OUTPUT/results.out
//install these code pre-requisites just in case
sudo apt update
sudo apt install python3-dev
sudo apt install default-jre
sudo apt install default-jdk
sudo apt-get install g++
#NOTE: As of RUNIT V1.0.8, we remove the maybe-ubiquity setting from the grub.cfg and loopback.cfg. We also removed the bottom code from the ubuntu.seed
